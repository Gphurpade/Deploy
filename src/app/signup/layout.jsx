
import Inputpart from "@/components/takeinput"

export default function SignupLayout({children}){
    return(
        <div className="h-screen w-screen text-center bg-red-100">
           {children}
          
        </div>
    );
}